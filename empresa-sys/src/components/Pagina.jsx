import Menu from "./Menu";

const Pagina = () => {
    return (
        <section className="pagina">
            <Menu />
            <section id="fondoInicial">
            </section>
        </section>
    );
};

export default Pagina;